name: flutter_algo_viz
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  lamont_labs_flutter_ui:
    git:
      url: https://github.com/drexel-ue/lamont_labs_flutter_ui.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
